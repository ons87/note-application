import { NoteTextFiltePipe } from './note-text-filte.pipe';

describe('NoteTextFiltePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteTextFiltePipe();
    expect(pipe).toBeTruthy();
  });
});
